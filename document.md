# Word to PDF Converter

โปรแกรมแปลงไฟล์ Word (.docx) เป็น PDF อัตโนมัติทั้งโฟลเดอร์

---

## 📋 ความต้องการของระบบ (System Requirements)

### ซอฟต์แวร์ที่จำเป็น:
| ซอฟต์แวร์ | เวอร์ชัน | หมายเหตุ |
|-----------|---------|----------|
| **Microsoft Word** | 2010 ขึ้นไป | **จำเป็นต้องติดตั้ง** - ใช้ในการแปลงไฟล์ |
| **Python** | 3.8+ | สำหรับรัน source code |
| **Windows** | 10/11 | รองรับ Windows เท่านั้น |

### Python Libraries:
```
pywin32
```

---

## 🚀 วิธีการติดตั้ง

### ตัวเลือกที่ 1: ใช้ไฟล์ .exe (แนะนำ)
1. ดาวน์โหลดไฟล์ `word_to_pdf_gui.exe` จาก [GitHub Releases](../../releases)
2. ดับเบิ้ลคลิกเพื่อรันโปรแกรม
3. ไม่ต้องติดตั้ง Python

### ตัวเลือกที่ 2: รัน Python Script
1. ติดตั้ง Python 3.8+
2. ติดตั้ง dependencies:
   ```bash
   pip install pywin32
   ```
3. รันโปรแกรม:
   ```bash
   python word_to_pdf_gui.py
   ```

---

## 📖 วิธีการใช้งาน

1. **เปิดโปรแกรม** - ดับเบิ้ลคลิก `word_to_pdf_gui.exe` หรือรัน Python script
2. **เลือกโฟลเดอร์** - คลิกปุ่ม "Browse" เพื่อเลือกโฟลเดอร์ที่มีไฟล์ .docx
3. **กดแปลง** - คลิกปุ่ม "🚀 เริ่มแปลงไฟล์"
4. **รอผลลัพธ์** - ไฟล์ PDF จะถูกสร้างในโฟลเดอร์เดียวกับไฟล์ Word

---

## 🔧 การ Build เป็น .exe

หากต้องการ build ไฟล์ .exe ใหม่:

```bash
# ติดตั้ง PyInstaller
pip install pyinstaller

# Build เป็น .exe
python -m PyInstaller --onefile --windowed word_to_pdf_gui.py
```

ไฟล์ .exe จะอยู่ในโฟลเดอร์ `dist/`

---

## 📁 ไฟล์ในโปรเจค

| ไฟล์ | คำอธิบาย |
|------|----------|
| `word_to_pdf_gui.py` | โปรแกรมหลัก (GUI version) |
| `word_to_pdf_converter.py` | โปรแกรม command-line version |

> 📦 ดาวน์โหลดไฟล์ `.exe` พร้อมใช้งานได้จาก [GitHub Releases](../../releases)

---

## ⚠️ ข้อจำกัด

- ต้องมี **Microsoft Word** ติดตั้งในเครื่อง
- รองรับเฉพาะ **Windows** เท่านั้น
- ไม่สามารถ deploy เป็น web app ได้ (เพราะต้องใช้ MS Word)

---

## 📝 หมายเหตุ

- โปรแกรมจะแปลงไฟล์ `.docx` ทั้งหมดในโฟลเดอร์ที่เลือก
- ไฟล์ PDF จะถูกบันทึกในโฟลเดอร์เดียวกับไฟล์ต้นฉบับ
- ชื่อไฟล์ PDF จะเหมือนกับไฟล์ Word (เปลี่ยนนามสกุลเป็น .pdf)
